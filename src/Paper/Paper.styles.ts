import { PaperProps } from '../..';
import { background } from '../OverqwilProvider/withBackgroundImage';

export const styles = {
  root: background,
};

export const defaultProps: PaperProps = {
  p: 'md',
};
